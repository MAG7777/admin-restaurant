// import { Roles } from 'constants/roles';

// import isAllowedRole from './role.guard';

// describe('Role guard', () => {
//     it('should return true for valid role', () => {
//         const allowedRoles = [Roles.Parent, Roles.Student];
//         const role = Roles.Parent;

//         expect(isAllowedRole(role, allowedRoles)).toBe(true);
//     });

//     it('should return false for invalid role', () => {
//         const allowedRoles = [Roles.Parent, Roles.Student];
//         const role = Roles.Guest;

//         expect(isAllowedRole(role, allowedRoles)).toBe(false);
//     });
// });
